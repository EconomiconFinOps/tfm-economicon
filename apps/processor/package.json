{
  "name": "@finops/processor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m app.run_all",
    "dev:api": "python -m app.run_api",
    "dev:worker": "python -m app.run_worker",
    "ingest:azure": "python -m app.run_azure_cost_ingestion",
    "build": "python -m compileall app",
    "lint": "python -m compileall app",
    "test": "python -m pytest tests",
    "docker:build": "docker build -t finops-processor ."
  }
}
