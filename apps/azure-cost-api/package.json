{
  "name": "@finops/azure-cost-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8002",
    "build": "python -m compileall app",
    "lint": "python -m compileall app",
    "test": "python -m pytest tests",
    "docker:build": "docker build -f Dockerfile -t finops-azure-cost-api ../.."
  }
}
