# Findings Backlog

Actualizacion RF-053-004 (2026-09-14): correccion integrada en `develop` por
la [PR #34](https://github.com/EconomiconFinOps/tfm-economicon/pull/34) el 13/09. La
[evidencia JUP-020](../../docs/evidence/JUP-020-validation.md) reproduce el
fallo anterior y acredita tres jobs completados por el flujo HTTP/RabbitMQ,
con 57 chunks y 57 embeddings persistidos. El historial de JUP-053 y su
smoke fallido se conserva; el alcance completo de JUP-020 sigue abierto.
La validacion operativa delegada de JUP-087 del 14/09 confirma otro job real
completado y registra RF-087-001/002 como defectos separados fuera de su
baseline de calidad. Paris conserva su rol y la auditoria personal pendiente.
La validacion funcional de Paris para RF-053-004 fue confirmada por el
usuario el 2026-09-14 junto con el cierre de JUP-020. El duplicado
RF-045-001 identificado en JUP-045 queda cubierto por el mismo arreglo.

| ID | Fecha | Origen | Tipo | Severidad | Scope | Estado | Owner | Accion | Change/Fix |
|----|-------|--------|------|-----------|-------|--------|-------|--------|------------|
| RF-082-001 | 2026-08-25 | jup-082-clean-develop | Repository hygiene | High | In scope | Fixed | Equipo Economicon | Remove personal agent configuration, vendor executables and duplicate task proposals while preserving functional changes | JUP-082 |
| RF-053-001 | 2026-09-09 | jup-053-secure-runtime-secrets | Effective DSN validation | High | In scope | Fixed | Equipo Economicon | Green3 rechaza overrides query y opciones no escalares; admite solo sslmode/connect_timeout/application_name. Regresiones contra parametros efectivos y mutantes detectados; review 2 confirma cierre. Evidencia: docs/evidence/JUP-053-validation.md. | JUP-053 |
| RF-053-002 | 2026-09-09 | jup-053-secure-runtime-secrets | Secret redaction | Medium | In scope | Fixed | Equipo Economicon | Green3 sanea claves de mappings raw/URL-encoded/form-encoded, conservando metadata segura y campos sensibles. Regresiones y mutantes detectados; review 2 confirma cierre. Evidencia: docs/evidence/JUP-053-validation.md. | JUP-053 |
| RF-053-003 | 2026-09-09 | jup-053-secure-runtime-secrets | Safe diagnostics | Medium | In scope | Fixed | Equipo Economicon | Green3 conserva la causa para logging final saneado, mantiene ingestion_failed persistido y nack. Prueba task-worker, mutante y smoke corroboran el diagnostico; review 2 confirma cierre. Evidencia: docs/evidence/JUP-053-validation.md. | JUP-053 |
| RF-053-004 | 2026-09-09 | jup-053-secure-runtime-secrets | Existing ingestion handoff defect | High | Out of scope (JUP-053) | Fixed | Equipo Economicon | Corregido por JUP-020 e integrado mediante PR #34 el 13/09 en cfc6668: el worker adapta el sobre id/payload al contrato job_id/text del grafo. La evidencia JUP-020 conserva el fallo previo y tres jobs completados; la validacion JUP-087 del 14/09 confirma otro job completado con documento/chunk/embedding persistidos. Validacion de Paris confirmada por el usuario el 2026-09-14. El alcance completo de JUP-020 y los providers reales siguen pendientes. | [PR #34](https://github.com/EconomiconFinOps/tfm-economicon/pull/34); [evidencia JUP-020](../../docs/evidence/JUP-020-validation.md); [JUP-020](https://trello.com/c/Mi3kPCOD). |
| RF-082-002 | 2026-08-25 | jup-082-clean-develop | Existing frontend lint baseline | Medium | In scope | Fixed | Equipo Economicon | **Resolución revisada en ADR-0003 (JUP-092):** no se corrigen manualmente las 49 violaciones; `react/prop-types` se desactiva para `.ts`/`.tsx` al adoptar TypeScript, condicionado a migrar los 9 `.jsx` afectados con cobertura real de tipos. **Cierre en JUP-087 (PR #29, aprobada por Lucia el 12/09; baseline revalidado el 14/09):** migra los nueve JSX y el resto del código fuente existente a TypeScript estricto con contratos reales de props y API; lint termina con cero errores y pruebas reales verifican los recorridos críticos. **Cierre paralelo en JUP-095 (grupo 8, tarea 8.3), en su propia rama:** de los mismos 9 archivos, 8 se migraron a `.tsx` a lo largo de los grupos 3-8 (dashboards, `Layout`, `LoginPage`, `IngestPage`, `ConversationsPage`, `DashboardPage`, `App`); el noveno, `PlaceholderPage.jsx`, se **eliminó** en vez de migrarse — verificado sin ningún consumidor. Ambos cierres son independientes y convergentes (mismo resultado observable, ADR-0003, código distinto); la reconciliación de ambas ramas (2026-09-19) hereda los ficheros/tests tipados de JUP-087 junto con el port de UI/router de JUP-095, sin violaciones de `react/prop-types` ni archivos `.jsx` restantes en `apps/frontend/src`. | ADR-0003 (Accepted, JUP-092); [Evidencia JUP-087](../../docs/evidence/JUP-087-validation.md); cierre paralelo en JUP-095 |
| RF-083-001 | 2026-08-19 | jup-083-clarify-frontend-migration-assumptions | OpenSpec validation | Medium | Out of scope | Fixed | Equipo Economicon | JUP-082 removed the duplicate proposals and restored strict global OpenSpec validation. | JUP-082 |
| RF-083-002 | 2026-08-19 | jup-083-clarify-frontend-migration-assumptions | Risk/scope | High | In scope | Open | Equipo Economicon | Integrar la experiencia visual del origen Figma Make, sin auth/capa de datos, preservando el login, tenant y contratos del destino inventariados en JUP-090. JUP-035 conserva el residual funcional y JUP-087 su calidad y regresion. TypeScript esta aprobado en ADR-0003 (JUP-092); su implementacion y la decision del salto de Vite 5 a 6 siguen pendientes en la reconciliacion de tooling. | JUP-035 / JUP-087 / ADR-0003 (JUP-092) |
| RF-090-001 | 2026-08-29 | jup-090-inventory-current-frontend | Platform/build reproducibility | Medium | Out of scope | Open | Equipo Economicon | `apps/frontend/Dockerfile` instala con `pnpm install --no-frozen-lockfile` sin copiar el lockfile ni el workspace de la raíz; construye fuera del lockfile reproducible que exige el spike. | Resolver en F4 `verificar-docker-compose` |
| RF-090-002 | 2026-08-29 | jup-090-inventory-current-frontend | Documentation accuracy | Low | Out of scope | Fixed | Equipo Economicon | El spike afirmaba `env_file: .env` en el servicio `frontend` de `docker-compose.yml`; esa clave no existe. | Corregido en JUP-090 |
| RF-090-003 | 2026-08-29 | jup-090-inventory-current-frontend | API contract drift | Medium | Out of scope | Fixed | Equipo Economicon | `GET /me` (`fetchProfile`) estaba documentado y contratado pero el frontend nunca lo invocaba; la identidad del operador viajaba solo en la respuesta de `POST /auth/login`, sin que el servidor revalidara nunca una sesión persistida en `localStorage`. **Resuelto en JUP-097 (grupo 3, decisión 2 de `design.md`):** `fetchProfile` se conecta —no se retira— en `SessionGate` como revalidación de la sesión persistida; su respuesta pasa a ser la identidad expuesta por `Outlet context`, en paralelo al bootstrap de tenants (mismo `enabled`, no en serie). Un token que el servidor rechaza en `/me` reutiliza el camino de fallo ya existente (`handleLogout` → `/login`), sin guard nuevo. | JUP-097 (`jup-097-reconcile-api-layer`), decisión 2 de `design.md` |
| RF-090-003 | 2026-08-29 | jup-090-inventory-current-frontend | API contract drift | Medium | Out of scope | Fixed | Equipo Economicon | `GET /me` (`fetchProfile`) estaba documentado y contratado pero el frontend nunca lo invocaba; la identidad del operador viajaba solo en la respuesta de `POST /auth/login`, sin que el servidor revalidara nunca una sesión persistida en `localStorage`. **Resuelto en JUP-097 (grupo 3, decisión 2 de `design.md`):** `fetchProfile` se conecta —no se retira— en `SessionGate` como revalidación de la sesión persistida; su respuesta pasa a ser la identidad expuesta por `Outlet context`, en paralelo al bootstrap de tenants (mismo `enabled`, no en serie). Un token que el servidor rechaza en `/me` reutiliza el camino de fallo ya existente (`handleLogout` → `/login`), sin guard nuevo. | JUP-097 (`jup-097-reconcile-api-layer`), decisión 2 de `design.md` |
| RF-091-001 | 2026-08-31 | jup-091-inventory-economicon-frontend | Documentation accuracy | Low | Out of scope | Fixed | Equipo Economicon | El supuesto T5 de JUP-083 describía el stack del origen como Tailwind + shadcn/ui + MUI 7; MUI y `@emotion/*` no se importan en ningún sitio. | Matizado en el spike dentro de JUP-091 |
| RF-091-002 | 2026-08-31 | jup-091-inventory-economicon-frontend | Dependency scope | Medium | Out of scope | Fixed | Equipo Economicon | Los 48 componentes shadcn/ui del origen son código muerto (ninguna pantalla los importa) y con ellos 38 de 61 dependencias; pero el destino sí necesita primitivos de formulario. **Resuelto en JUP-094:** se adopta un subconjunto de 6 paquetes Radix (`react-label`, `react-select`, `react-slot`, `react-separator`, `react-dialog`, `react-tooltip`), no los 26 del origen ni los otros 7 de apoyo shadcn. Motivo registrado en [ADR-0004](../../docs/adr/ADR-0004-frontend-shadcn-ui.md): facilidad de desarrollo para F3 (login, tenant, ingesta, chat necesitan primitivos accesibles), no paridad con el origen — el inventario confirma que el origen tampoco los renderiza. Copiar el código de cada componente a `src/components/ui/` sigue pendiente, componente por componente, en F3. | ADR-0004 (Accepted); JUP-094 |
| RF-091-003 | 2026-08-31 | jup-091-inventory-economicon-frontend | Backend capability gap | High | Out of scope | Open | Equipo Economicon | Siete capacidades de backend ausentes (serie temporal, desglose por dimensión, multi-cloud AWS/GCP, acciones de recorte, anomalías, recomendaciones, inventario de recursos): solo 2 de 14 datos del origen tienen contrato y ambos parciales; 4 de 5 pantallas no tienen ninguno. | Decisión de épica sobre el alcance de F3; C1 y C2 son las más baratas (dato ya en BD) |
| RF-091-004 | 2026-08-31 | jup-091-inventory-economicon-frontend | Mock data in production path | Medium | Out of scope | Open | Equipo Economicon | `GET /billing/summary` devuelve `monthly_spend` y `savings_identified` hardcodeados; solo `open_ingestions` es real, pese a que el processor ya ingesta costes Azure a CockroachDB. | Conectar el endpoint a los datos ingestados como prerrequisito del dashboard real |
| RF-093-001 | 2026-09-06 | jup-093-configure-typescript | Local dev tooling | Low | Out of scope | Open | Equipo Economicon | En al menos una máquina Windows del equipo, `corepack pnpm <script>` en la raíz falla para cualquier script orquestado por turbo (`lint`, `build`, `test`, y ahora `typecheck`): turbo resuelve pnpm `v11.9.0` en los subprocesos por paquete, pese a que `packageManager: pnpm@9.0.0` resuelve correctamente en shell interactiva (`corepack pnpm --version` y `pnpm --version` dan `9.0.0`). Confirmado preexistente a JUP-093: `pnpm lint`/`pnpm build` ya fallaban igual sobre `@finops/backend`/`@finops/processor`, paquetes no tocados por esta tarjeta. No confirmado si ocurre en CI (que hace `corepack enable` limpio en cada job) ni en otras máquinas del equipo. Verificación de JUP-093 sustituida por `corepack pnpm --filter @finops/frontend <script>`, que no pasa por turbo. | Investigar en tarjeta aparte si el equipo lo reproduce; posible pista: resolución de `packageManager` por turbo en subprocesos vs. shell interactiva |
| RF-044-001 | 2026-09-08 | jup-044-e2e-tracing | Concurrency bug / reliability | High | Out of scope | Fixed | Equipo Economicon | `apps/processor/app/run_all.py` arranca el hilo del worker (`ProcessorWorker.run_forever`) y el servidor uvicorn en paralelo; ambos llaman a `Database.initialize()` (que ejecuta las migraciones) de forma concurrente, sin ninguna coordinación. El que pierde la carrera revienta con `UniqueViolation: duplicate key ... processor_schema_migrations_pkey` al intentar reinsertar una migración que el otro ya registró. Como ocurre en un hilo demonio, la excepción no mata el contenedor: el proceso sigue "healthy" (uvicorn vivo) pero el worker queda muerto en silencio — el processor deja de consumir jobs sin ningún síntoma visible salvo el traceback en el log. Reproducido de forma determinista en `docker compose up`/`restart processor` repetidos durante la verificación manual de JUP-044 (varios arranques consecutivos, resultado distinto cada vez — es una carrera real, no un evento raro). **Resuelto en PR #16 (JUP-049):** `MigrationRunner.run()` ahora serializa toda la ejecución con un `threading.Lock` de proceso, cubriendo el hilo del worker y el arranque de uvicorn del mismo contenedor; verificado con test de concurrencia dedicado y confirmado en vivo (build y arranque desde cero). | JUP-049 (PR #16)
| RF-044-002 | 2026-09-08 | jup-044-e2e-tracing | Concurrency bug / reliability | High | Out of scope | Open | Equipo Economicon | Mismo patrón de causa raíz que `RF-044-001` pero entre servicios: `backend` y `processor` ejecutan sus propias migraciones (`MigrationRunner`) de forma independiente al arrancar, sin ningún lock, contra la misma CockroachDB compartida. En un arranque en frío de todo el stack (volumen recién creado), ambos compiten por crear/alterar tablas al mismo tiempo, y CockroachDB responde con `SerializationFailure`/`WriteTooOldError: TransactionRetryWithProtoRefreshError` en el perdedor, tumbando el contenedor entero (a diferencia de `RF-044-001`, aquí sí es fatal porque ocurre en el hilo principal de arranque de uvicorn). Reproducido de forma determinista durante la verificación manual de JUP-044 al recrear los volúmenes de Docker. | Pendiente de tarjeta de migraciones (Trello asigna actualmente JUP-096 a esta épica; no confundir con la referencia histórica de JUP-095 a "JUP-096" para la reconciliación de la capa de API)
| RF-045-001 | 2026-09-12 | jup-045-ingest-failure-alerts | Correctness / broken core path | High | Out of scope | Fixed (duplicado, ver JUP-020) | Equipo Economicon | Toda ingesta de texto real fallaba: `Database.create_job()` (`apps/backend/app/db/database.py`) devuelve el payload original en `job["payload"]` y publica ese `job` (sin `text_content`/`metadata` a nivel superior) en RabbitMQ. El worker del processor pasaba ese mismo `job` tal cual a `PipelineRunner.run(job)` → `graph.invoke(payload)` (`apps/processor/app/graphs/pipeline.py`), cuyo primer nodo (`_chunk`) esperaba `state["text_content"]` a nivel superior y reventaba con `KeyError: 'text_content'`. Reproducido de forma determinista enviando `POST /jobs/ingest` real durante la verificación manual de JUP-045. **No era un hallazgo nuevo**: mismo residual de contrato ya registrado en la tarjeta JUP-020 desde el 09/09/2026 (detectado durante JUP-044/PR #30, ver también `RF-053-004`). **Corregido en PR #34 (JUP-020)**, mergeado a `develop`: `IngestTask.execute()` mapea explícitamente el envelope antes de invocar el pipeline; verificado con tests de regresión que ejercitan el productor real del backend contra el pipeline real, y con `POST /jobs/ingest` real completando la ingesta. No relacionado con el alertado de JUP-045 en sí (que sigue siendo válido: alerta correctamente sobre fallos reales, incluido este). | JUP-020 (PR #34)
| RF-095-001 | 2026-09-12 | jup-095-portar-codigo-fuente | Backend CORS missing | High | Out of scope | Open | Equipo Economicon | `apps/backend` no tiene configurado `CORSMiddleware` (ni ninguna cabecera CORS): confirmado con `grep -i cors` sobre todo `apps/backend`, cero resultados. Cualquier navegador real que sirva el frontend en un origen distinto del backend (p. ej. `localhost:5173` vs `localhost:8000`, exactamente la topología de `docker-compose.yml` y de `pnpm dev`) bloquea `POST /auth/login` y el resto de llamadas con `Access to fetch ... blocked by CORS policy`, aunque `curl`/Postman funcionen (no aplican CORS). Descubierto durante la verificación manual E2E de la tarea 6.6 de JUP-095 (login con Playwright + Chromium real contra el backend levantado con `docker compose`): la petición de login falla en el navegador; verificado con `--disable-web-security` que el resto del recorrido (login → tenant → `/overview-legacy` con datos reales → cambio de tenant sobrevive a la navegación → logout) funciona correctamente una vez sorteado el bloqueo. No es una regresión de JUP-095 (el frontend nunca configura CORS, es responsabilidad exclusiva del backend) ni preexistía documentado en ningún finding anterior. Coincide con `RF-087-001` (mismo síntoma, observado independientemente desde JUP-087). | Añadir `CORSMiddleware` en `apps/backend/app/main.py` permitiendo el origen del frontend (`VITE_API_BASE_URL`/`FRONTEND_PORT`); tarjeta aparte, backend fuera del alcance de JUP-095 y JUP-087 |
| RF-095-002 | 2026-09-12 | jup-095-portar-codigo-fuente | Mock data in production path | Medium | Out of scope | Open | Equipo Economicon | Las 5 pantallas de coste portadas del origen (`ExecutiveCostDashboard`, `OperationalCostDashboard`, `ExecutiveCutDashboard`, `AnomaliesPanel`, `RecommendationsPanel`) muestran datos de demostración estáticos (`apps/frontend/src/data/demo/*.ts`, extraídos en la tarea 5.2, cada módulo señalizado con el comentario grep-able "DATOS DE DEMOSTRACION (sustituibles)"), no datos reales del backend — decisión 1 y non-goal explícito de `design.md` de JUP-095 (portar presentación y enrutado, no conectar datos). Solo `/overview-legacy` (`DashboardPage`, resumen de facturación) consume datos reales de `GET /billing/summary` y `GET /health`. **Refinado en JUP-097 (grupo 6):** cada uno de los 16 datos de las 5 pantallas queda nombrado, dato a dato, junto a la capacidad concreta de `RF-091-003` que le falta (C1 serie temporal, C2 desglose por dimensión, C3 multi-cloud, C4 objetivos y acciones de recorte, C5 anomalías, C6 recomendaciones, C7 inventario de recursos) — ver [docs/planning/JUP-097-frontend-data-gap-map.md](../../docs/planning/JUP-097-frontend-data-gap-map.md). El mismo mapa se replicó en el comentario de cada módulo `src/data/demo/*.ts`. **Permanece `Open` deliberadamente**: JUP-097 no conecta ninguna pantalla nueva (decisión de alcance previa a proponer — no se toca backend), así que el finding no se cierra, solo se refina como insumo accionable. | Conectar cada pantalla a datos reales queda condicionado a la decisión de épica sobre `RF-091-003`; el mapa de JUP-097 es el insumo para tomarla |
| RF-087-001 | 2026-09-14 | jup-087-frontend-quality-baseline | Browser/API integration | High | Out of scope (JUP-087) | Open | Equipo Economicon | La configuracion canonica de frontend/API entre puertos devuelve OPTIONS /auth/login 405 sin Access-Control-Allow-Origin. Un proxy de mismo origen exclusivo permite el ensayo, pero no corrige el producto. Definir la configuracion soportada y verificar preflight/login con navegador real, sin desactivar protecciones del navegador. Coincide con `RF-095-001` (mismo síntoma, observado independientemente desde JUP-095). | [JUP-085](https://trello.com/c/Z8M443Hu); [evidencia](../../docs/evidence/JUP-087-validation.md). |
| RF-087-002 | 2026-09-14 | jup-087-frontend-quality-baseline | Conversation history persistence contract | High | Out of scope (JUP-087) | Open | Equipo Economicon | Tras POST mensaje 201, GET historial falla 500: Database.fetch_messages aplica json.loads a metadata JSONB ya deserializada como dict por psycopg. Reproducido sin proxy y con mensajes persistidos; linea heredada del 14/06. Normalizar lectura y probar envio seguido de recarga con CockroachDB real. La interfaz muestra el error y el tenant incorrecto recibe 404. | [JUP-035](https://trello.com/c/BJfABykd); [evidencia](../../docs/evidence/JUP-087-validation.md). |
