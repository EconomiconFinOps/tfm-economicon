# Findings Backlog

| ID | Fecha | Origen | Tipo | Severidad | Scope | Estado | Owner | Accion | Change/Fix |
|----|-------|--------|------|-----------|-------|--------|-------|--------|------------|
| RF-082-001 | 2026-08-25 | jup-082-clean-develop | Repository hygiene | High | In scope | Fixed | Equipo Economicon | Remove personal agent configuration, vendor executables and duplicate task proposals while preserving functional changes | JUP-082 |
| RF-082-002 | 2026-08-25 | jup-082-clean-develop | Existing frontend lint baseline | Medium | Out of scope | Open | Equipo Economicon | **Resolución revisada en ADR-0003 (JUP-092), corregida en revisión de PR (2026-09-04):** no se corrigen manualmente las 49 violaciones; `react/prop-types` se desactiva para `.ts`/`.tsx` al adoptar TypeScript. Pero las 49 violaciones viven en 9 archivos `.jsx`, que F2 no toca ni renombra — solo instala tooling. La regla les sigue aplicando tras F2, así que **el finding permanece `Open` hasta que F3 (o el cierre de F5) migre esos 9 archivos a `.tsx` con cobertura real de tipos**; no se cierra como efecto de F2. | ADR-0003 (Accepted); ejecución y cierre material pendientes en F3/F5 |
| RF-083-001 | 2026-08-19 | jup-083-clarify-frontend-migration-assumptions | OpenSpec validation | Medium | Out of scope | Fixed | Equipo Economicon | JUP-082 removed the duplicate proposals and restored strict global OpenSpec validation. | JUP-082 |
| RF-083-002 | 2026-08-19 | jup-083-clarify-frontend-migration-assumptions | Risk/scope | High | Out of scope | Open | Equipo Economicon | Frontend de Economicon es dashboard estático (Figma Make) sin backend/auth/capa de datos; F3 debe añadir esas capas desde el destino. Evaluar Vite 6 vs 5 y TSX sin tsconfig. | Pending Trello triage |
| RF-090-001 | 2026-08-29 | jup-090-inventory-current-frontend | Platform/build reproducibility | Medium | Out of scope | Open | Equipo Economicon | `apps/frontend/Dockerfile` instala con `pnpm install --no-frozen-lockfile` sin copiar el lockfile ni el workspace de la raíz; construye fuera del lockfile reproducible que exige el spike. | Resolver en F4 `verificar-docker-compose` |
| RF-090-002 | 2026-08-29 | jup-090-inventory-current-frontend | Documentation accuracy | Low | Out of scope | Fixed | Equipo Economicon | El spike afirmaba `env_file: .env` en el servicio `frontend` de `docker-compose.yml`; esa clave no existe. | Corregido en JUP-090 |
| RF-090-003 | 2026-08-29 | jup-090-inventory-current-frontend | API contract drift | Medium | Out of scope | Open | Equipo Economicon | `GET /me` (`fetchProfile`) está documentado y contratado pero el frontend nunca lo invoca; la identidad del operador viaja en la respuesta de `POST /auth/login`. | Decidir en F3 `reconciliar-capa-api` |
| RF-091-001 | 2026-08-31 | jup-091-inventory-economicon-frontend | Documentation accuracy | Low | Out of scope | Fixed | Equipo Economicon | El supuesto T5 de JUP-083 describía el stack del origen como Tailwind + shadcn/ui + MUI 7; MUI y `@emotion/*` no se importan en ningún sitio. | Matizado en el spike dentro de JUP-091 |
| RF-091-002 | 2026-08-31 | jup-091-inventory-economicon-frontend | Dependency scope | Medium | Out of scope | Fixed | Equipo Economicon | Los 48 componentes shadcn/ui del origen son código muerto (ninguna pantalla los importa) y con ellos 38 de 61 dependencias; pero el destino sí necesita primitivos de formulario. **Resuelto en JUP-094:** se adopta un subconjunto de 6 paquetes Radix (`react-label`, `react-select`, `react-slot`, `react-separator`, `react-dialog`, `react-tooltip`), no los 26 del origen ni los otros 7 de apoyo shadcn. Motivo registrado en [ADR-0004](../../docs/adr/ADR-0004-frontend-shadcn-ui.md): facilidad de desarrollo para F3 (login, tenant, ingesta, chat necesitan primitivos accesibles), no paridad con el origen — el inventario confirma que el origen tampoco los renderiza. Copiar el código de cada componente a `src/components/ui/` sigue pendiente, componente por componente, en F3. | ADR-0004 (Accepted); JUP-094 |
| RF-091-003 | 2026-08-31 | jup-091-inventory-economicon-frontend | Backend capability gap | High | Out of scope | Open | Equipo Economicon | Siete capacidades de backend ausentes (serie temporal, desglose por dimensión, multi-cloud AWS/GCP, acciones de recorte, anomalías, recomendaciones, inventario de recursos): solo 2 de 14 datos del origen tienen contrato y ambos parciales; 4 de 5 pantallas no tienen ninguno. | Decisión de épica sobre el alcance de F3; C1 y C2 son las más baratas (dato ya en BD) |
| RF-091-004 | 2026-08-31 | jup-091-inventory-economicon-frontend | Mock data in production path | Medium | Out of scope | Open | Equipo Economicon | `GET /billing/summary` devuelve `monthly_spend` y `savings_identified` hardcodeados; solo `open_ingestions` es real, pese a que el processor ya ingesta costes Azure a CockroachDB. | Conectar el endpoint a los datos ingestados como prerrequisito del dashboard real |
| RF-093-001 | 2026-09-06 | jup-093-configure-typescript | Local dev tooling | Low | Out of scope | Open | Equipo Economicon | En al menos una máquina Windows del equipo, `corepack pnpm <script>` en la raíz falla para cualquier script orquestado por turbo (`lint`, `build`, `test`, y ahora `typecheck`): turbo resuelve pnpm `v11.9.0` en los subprocesos por paquete, pese a que `packageManager: pnpm@9.0.0` resuelve correctamente en shell interactiva (`corepack pnpm --version` y `pnpm --version` dan `9.0.0`). Confirmado preexistente a JUP-093: `pnpm lint`/`pnpm build` ya fallaban igual sobre `@finops/backend`/`@finops/processor`, paquetes no tocados por esta tarjeta. No confirmado si ocurre en CI (que hace `corepack enable` limpio en cada job) ni en otras máquinas del equipo. Verificación de JUP-093 sustituida por `corepack pnpm --filter @finops/frontend <script>`, que no pasa por turbo. | Investigar en tarjeta aparte si el equipo lo reproduce; posible pista: resolución de `packageManager` por turbo en subprocesos vs. shell interactiva |
| RF-095-001 | 2026-09-12 | jup-095-portar-codigo-fuente | Backend CORS missing | High | Out of scope | Open | Equipo Economicon | `apps/backend` no tiene configurado `CORSMiddleware` (ni ninguna cabecera CORS): confirmado con `grep -i cors` sobre todo `apps/backend`, cero resultados. Cualquier navegador real que sirva el frontend en un origen distinto del backend (p. ej. `localhost:5173` vs `localhost:8000`, exactamente la topología de `docker-compose.yml` y de `pnpm dev`) bloquea `POST /auth/login` y el resto de llamadas con `Access to fetch ... blocked by CORS policy`, aunque `curl`/Postman funcionen (no aplican CORS). Descubierto durante la verificación manual E2E de la tarea 6.6 de JUP-095 (login con Playwright + Chromium real contra el backend levantado con `docker compose`): la petición de login falla en el navegador; verificado con `--disable-web-security` que el resto del recorrido (login → tenant → `/overview-legacy` con datos reales → cambio de tenant sobrevive a la navegación → logout) funciona correctamente una vez sorteado el bloqueo. No es una regresión de JUP-095 (el frontend nunca configura CORS, es responsabilidad exclusiva del backend) ni preexistía documentado en ningún finding anterior. | Añadir `CORSMiddleware` en `apps/backend/app/main.py` permitiendo el origen del frontend (`VITE_API_BASE_URL`/`FRONTEND_PORT`); tarjeta aparte, backend fuera del alcance de JUP-095 |
