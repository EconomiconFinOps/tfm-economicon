## 1. Demo Credential Normalization

- [x] 1.1 Replace the backend auth schema test email with `operator@example.com`.
- [x] 1.2 Replace the backend seeded demo operator email with `operator@example.com`.
- [x] 1.3 Replace frontend demo login defaults and active README credential references with `operator@example.com`.
- [x] 1.4 Update `RF-004-001` in `openspec/findings/backlog.md` to link this HU and reflect the fix status.

## 2. Validation

- [x] 2.1 Run focused backend auth schema tests.
- [x] 2.2 Run OpenSpec validation and HU anti-harness checks.
- [x] 2.3 Record review findings and post-review approval before archive.
