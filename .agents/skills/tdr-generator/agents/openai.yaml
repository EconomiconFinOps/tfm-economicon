interface:
  display_name: "SDD TDR Generator"
  short_description: "Diseña decisiones técnicas trazables"
  default_prompt: "Use $tdr-generator con el input preparado por el harness para producir el TDR."
policy:
  allow_implicit_invocation: false
